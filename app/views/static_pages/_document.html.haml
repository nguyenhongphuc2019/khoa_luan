- if user_signed_in?
  %li
    = link_to(document.name, document_path(document), class: "document", data: {document_id: document.id, user_id: current_user.id})
- else
  %li
    = link_to(document.name, document_path(document), class: "document")  
  